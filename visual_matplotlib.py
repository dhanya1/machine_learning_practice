import matplotlib.pyplot as plt
plt.scatter(x,y,s=<>, c = , a) #Scatterplot , s = means size.. store what the bubbles shud be proportinal to in a numpy array
#c= color .. store color corresponding to a factor in a dictionary
plt.plot(x,y) #to get a line plot
plt.hist(x, bins=3) #plot a histogram
plt.xlabel()
plt.ylabel()
plt.title()